#include <stdio.h>
#include <string.h>


int socket () {

int socket_serveur;

socket_serveur = socket(AF_INET, STOCK_STREAM,0);

 if (socket_serveur == -1) {

   perror("socket_serveur");
   
 }


 

  }
